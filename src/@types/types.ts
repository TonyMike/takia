export interface selectProp {
  label: string;
  name: string;
  options: string[];
  required?: boolean;
  handleChange?:any
}

export interface categoryProp {
  category: string;
  icon: string;
}
// export stringProp
